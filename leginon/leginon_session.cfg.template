# session configuration
[name]
# prefix of session names. This is added before the standard suggested name.
# For example,
# prefix: n
# makes session name 20mar20a becomes n20mar20a
prefix: 
[app]
# a list of prefix,separated by ",", for applications to show in application history.
# For example,
# prefix: MSI-Tom, Cal
prefix: 
