// Generic functions that work on C-contiguous bits of memory

void convolveWithSymmetricKernel( f64 *input , u64 input_size, f64 *kernel, u64 kernel_radius ) {
	
	u32 i, k;
	f64 *tmp = NEWV(f64,input_size);
	
	for(i=0;i<inputsize;i++) {
		
	}
	
}